# Install & run

## Front

`npm install`
`npm run start`

## Back

npm install

install mysql and run service

`cp .env.dist .env` and change if necessary

### Run dev
`npm run fixtures:load`
`npm run start:dev`

### Run prod
`npm run start:prod`
